export default class Floor
{
    index: number;
    id: string;
    name: string;

    constructor(index: number, id: string, name: string) {
        this.index = index;
        this.id = id;
        this.name = name;
    }
}